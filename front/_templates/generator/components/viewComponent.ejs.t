---
to: src/components/<%= atomic %>/<%= component_name %>/<%= component_name %>View.vue
---

<script setup lang="ts">
// import DefinedComponent from "../path"

const props = defineProps<{
  // // propsを定義
  // propsVal: DefinedType,
  // propsFunc: (value: DefinedType) => void
}>()
</script>

<template>
  <!-- // <DefinedComponent>
    {{[propsVal]}}
  </DefinedComponent> -->
</template>